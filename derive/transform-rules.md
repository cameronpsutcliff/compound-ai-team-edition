# Transform rules for derive.sh

Reproducible scrub transforms applied when deriving Individual and Team editions
from the Mini canonical. Downstream trees are generated by `derive/derive.sh`,
never hand-edited. If a transform changes, update this file and re-run derive.

Authority: `attribution-allowlist.txt` and `leak-denylist.txt` on the canonical.
The derive step applies **denylist-minus-allowlist**: strip allowlisted tokens from
each line, then drop any line that still matches a denylist term or structural
personal-path pattern. This matches `enforcement/bin/check-portability.sh`.

## 1. Version strings

Version strings are NOT blanket-substituted. A global old-version to
new-version rewrite corrupts history: it turns `## v2.7.0 Changes` changelog
headers and the `v2.7.0: Cameron Sutcliff solo edition` attribution into the
current version, and mangles upgrade-mechanic examples like `v2.6.0 -> v2.7.0`.

Current-version stamps are bumped at the source in the canonical (the per-file
`# Compound AI Operating Standards vX.Y.Z` header, `version:` fields,
`kit_version:`, and shell-skill page stamps). Legacy version references that
remain in the derived tree are deliberate: version history, the solo-edition
attribution, and illustrative upgrade examples in the adoption-captain
reference docs. The release version of record is in `CITATION.cff`.

## 2. Path and coupling rewrites

Token substitutions (no-op when absent), inherited from the canonical
`scrub-for-public.py` derive step:

| From | To |
|------|-----|
| `app.db` | `app.db` |
| `ENHANCEMENTS.md` | `ENHANCEMENTS.md` |
| `Keep internal repo paths out of the public copy.` | `Keep internal repo paths out of the public copy.` |
| `several agentic systems on one machine` | `several agentic systems on one machine` |
| `on one machine` | `on one machine` |
| `a directory site` | `a directory site` |
| `ghostwrite a social presence` | `ghostwrite a social presence` |
| `paper-trade a strategy` | `paper-trade a strategy` |
| `,` | `,` |
| ` and` | ` and` |

Structural drops:

- Remove the entire `### Appendix E. Citation Index` section through the next
  `### Appendix ` or `## ` heading (personal source paths).
- Drop any line matching the hard-path safety net:
  absolute user-home paths, mounted private volumes, private workspace roots,
  private vault names, private repository names, private database names,

## 3. Denylist-minus-allowlist scrub

After version and token rewrites, for each text file:

1. Load terms from `attribution-allowlist.txt` (longest first).
2. Load terms from `leak-denylist.txt` / `forbidden-terms.txt`.
3. For each line: remove every allowlisted substring, then discard the line if it
   still contains any denylist term or personal filesystem path.

Allowlisted co-author identities **must survive**: `Joshua Sutcliff`,
`joshuadsutcliff`, `Cameron Sutcliff`, `cameronpsutcliff`, and the public System B
reference `github.com/joshuadsutcliff`.

## 4. Edition omissions

Two categories. First, internal build-process and private-deployment docs that
never ship in EITHER edition. The Team edition ignores `exclude.txt`, so these
are dropped in `derive.sh` directly (the `should_skip_copy` always-skip case):

- `docs/FINAL-REVIEW.md`, `docs/HG-2-DEBATE.md`, `docs/PUBLICATION-CHECKLIST.md`,
  `docs/WEBSITE-COPY.md`, `docs/FRESH-SESSION-DRY-RUN.md`,
  `docs/component-ledger.md`, `docs/derived-not-typed.md`, `docs/brief-standard.md`
- `leak-denylist.local.txt`

Second, files copied into the derived tree and shipped in the Team edition but
omitted from the Individual zip by `exclude.txt` (the lean adopter payload):

- `AGENT.annotated.md`, `adoption-report.md`, `delegation.md`,
  `project-scaffold.md`, `project-internal-structure.md`
- `_reference/josh-merge/blend-system.md` (design input)
- `team/` (the Team org layer)
- `derive/` (the build tooling)

`reference-impl/` and `doctrine/conventions/universal-skill-routing.md` ship in
BOTH editions and are intentionally not omitted.

## 5. Manifest regeneration

After copy and scrub, derive regenerates:

- `compound-ai.manifest.json` (file list, per-file sha256, aggregate hash, version)
- `compound-ai.sha256` (plain-text checksum listing)

Implementation: seed manifest metadata, then run
`reference-impl/scripts/build-manifest.py` in the derived tree.

## 6. Release zip profiles (D-002)

| Edition | Zip contents |
|---------|----------------|
| `individual` | Full derived tree minus `exclude.txt` entries, including `reference-impl/` |
| `team` | Full derived tree, with no `exclude.txt` omissions |

Zip output name: `compound-ai-starter-kit-v<version>.zip` in the destination directory.

## 7. Validation

1. `enforcement/bin/check-kit.sh` on the derived tree (all gates PASS).
2. `enforcement/bin/simulate-fresh-clone.sh <derived-tree>` (fresh-copy simulation).
3. `enforcement/bin/check-portability.sh` confirms allowlisted names present and denylist absent.
